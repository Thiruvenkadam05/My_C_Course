#include<stdio.h>
main()
{
	float me = 1.1;
	double you = 1.1;
	if(me == you)
		printf("you are an idiot");
	else
		printf("u r a gud guy ");
};

//in case of non-terminating decimals f and d are not equal
//in case of terminating decimals f and d are equal   
